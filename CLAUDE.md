# Project notes

## Source of truth
All design edits go into **`Exponent - Redesign A (Signal).dc.html`**. This is the only file to change.

## Build output — never edit
- `Exponent Assembly - Website.html` — standalone single-file export for preview/download only.
- `export-assets/` — downscaled JPEG copies used only by that export.

Both are regenerated from the source file on request. Never edit them directly, and never treat them as the current design.
